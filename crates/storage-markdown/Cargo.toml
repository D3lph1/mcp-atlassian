[package]
name = "storage-markdown"
description = "Confluence storage format (XHTML) <-> Markdown conversion"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
comrak = { version = "0.54.0", default-features = false }
htmd = "0.5.5"
similar = { version = "3.2.0", default-features = false, features = ["text", "inline"] }

[lints]
workspace = true
